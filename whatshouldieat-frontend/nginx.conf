events{}
http {
    include /etc/nginx/mime.types;
    server {
        listen 0.0.0.0:80891;
        listen [::]:80891 ipv6only=on default_server;
        server_name localhost;
        root /usr/share/nginx/html;
        index index.html;
        location / {
            try_files $uri $uri/ /index.html;
        }
    }
}
