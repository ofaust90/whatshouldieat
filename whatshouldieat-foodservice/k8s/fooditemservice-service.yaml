apiVersion: v1
kind: Service
metadata:
  name: fooditemservice-service
  labels:
    name: fooditemservice-service
    app: whatshouldieat
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    name: fooditemservice-pod
    app: whatshouldieat
